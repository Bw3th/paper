#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_ng-annotate@0.15.4@ng-annotate/build/es5/ng-annotate" "$@"
  ret=$?
else 
  node  "$basedir/../../../_ng-annotate@0.15.4@ng-annotate/build/es5/ng-annotate" "$@"
  ret=$?
fi
exit $ret
